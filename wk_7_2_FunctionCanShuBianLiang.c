/*
函数先后关系
    把sum写在上面，因为
        c编译器自上而下分析你的代码
C99以前猜测 int sum(int,int)
将函数头前置-函数原型声明
函数定义

参数传递
函数不会对外部变量有影响，只能接受和返回值

变量空间

本地变量

在块中定义，在块中生存和作用

逗号运算符,结果是后面

return(i); //误解return 是个函数

批处理文件
    win: if errorlevel 1 ..
    unix bash: echo $?
    csh:echo $status

*/

